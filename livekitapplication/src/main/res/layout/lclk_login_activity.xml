<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical">

        <EditText
            android:id="@+id/activity_login_et_username"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:gravity="center_vertical"
            android:maxLength="50"
            android:paddingLeft="30dp"
            android:singleLine="true"
            android:text="Tom"
            android:textAppearance="@style/LCLKTextAppearance.Normal"
            android:textColor="@color/lclk_common_gray"/>

        <Button
            android:id="@+id/activity_login_btn_login"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_marginLeft="20dp"
            android:layout_marginRight="20dp"
            android:layout_marginTop="10dp"
            android:background="@color/lclk_common_blue"
            android:text="@string/lclk_login"
            android:textAppearance="@style/LCLKTextAppearance.Normal.Bold"
            android:textColor="@android:color/white"/>
    </LinearLayout>
</ScrollView>